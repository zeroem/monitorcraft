- hosts: all
  sudo: yes

  vars_files:
    - shared-vars.yaml

  roles:
    - role: riemann-tools
    - role: graphite
    - role: logstash
      logstash_run_as: root
      logstash_conf_template: "logstash.d/graphite.conf.j2"
